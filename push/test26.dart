void main() {
  final DateTime now = DateTime.now();
  final DateTime today = DateTime(now.year, now.month, now.day);
  final DateTime sevenDaysLater = DateTime.fromMillisecondsSinceEpoch(1782437070267);

  // print(now.add(Duration(hours: 5)));
  print((1 / 12).ceil());
}
